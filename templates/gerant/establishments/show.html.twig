{% extends 'base.html.twig' %}

{% block title %}Establishments{% endblock %}

{% block body %}
    <h1>Establishments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ establishment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ establishment.name }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ establishment.city }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ establishment.address }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ establishment.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('establishments_list') }}">back to list</a>

    <a href="{{ path('establishments_list_edit', {'id': establishment.id}) }}">edit</a>

    {{ include('gerant/establishments/_delete_form.html.twig') }}
{% endblock %}
