<form method="post" action="{{ path('reservations_list_delete', {'id': reservation.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
    <button type="submit" class="btn">Supprimer</button>
</form>
