{% extends 'base.html.twig' %}

{% block title %}Suites{% endblock %}

{% block body %}

    <div class="container-fluid text-center" style="padding-top: 50px">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-8">
                    <h1>Suite {{ suite.title }}</h1>
                    <h5>{{ suite.description }}</h5>
                </div>
            </div>
        </div>
    </div>


    <div class="container-fluid text-center">
        <table id="datatablesSimple">
            <tbody>
            <tr>
                <th></th>
                <td>
                    {% for image in images %}
                        <img src={{ asset(image) }} alt="Image" width="300rem">
                    {% endfor %}
                </td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="container" style="padding-top: 150px ">
        <a class="btn btn-sm btn-primary rounded py-2 px-4" href="{{ path('customer_establishments_list') }}">Retour à la liste</a>
    </div>

{% endblock %}
