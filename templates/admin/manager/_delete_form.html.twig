<form method="post" action="{{ path('manager_list_delete', {'id': manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manager.id) }}">
    <button class="btn btn-outline-info">Supprimer</button>
</form>
