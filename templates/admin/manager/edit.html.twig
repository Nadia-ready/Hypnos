{% extends 'admin/base.html.twig' %}

{% block title %}Modifier un gérant{% endblock %}

{% block body %}
    <h1>Modifier un gérant</h1>

    <div>
        {{ include('admin/manager/_form.html.twig') }}
    </div>
    <div class="btn-group">
        <div>
            <button class="btn btn-outline-info">{{ button_label|default('Enregister') }}</button>

        </div>

        <div>
            {{ include('admin/manager/_delete_form.html.twig') }}
        </div>

        <div>
            <a href="{{ path('manager_list') }}" class="btn btn-outline-info">Retour à la liste</a>
        </div>



    </div>




{% endblock %}
