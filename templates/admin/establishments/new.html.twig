{% extends 'admin/base.html.twig' %}

{% block title %}Nouveau établissement{% endblock %}

{% block body %}
    <h1>Créer un nouvel établissement</h1>

    {{ include('admin/establishments/_form.html.twig') }}

    <div class="btn-group">
        <div>
            <button class="btn btn-outline-info">{{ button_label|default('Enregistrer') }}</button>
        </div>

        <div>
            <a href="{{ path('establishments_list') }}">Retour à la liste</a>
        </div>


    </div>


{% endblock %}
