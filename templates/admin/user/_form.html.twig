{{ form_start(form) }}
<div class="card mb-4">
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        Utilisateurs
    </div>
    <div class="card-body">
        {{ form_widget(form) }}

{{ form_end(form) }}
