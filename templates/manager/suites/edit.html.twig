{% extends 'manager/base.html.twig' %}

{% block title %}Modifier la suite{% endblock %}

{% block body %}
    <h1>Modifier la suite</h1>

    <div>
        {{ include('manager/suites/_form.html.twig') }}
    </div>
    <div class="btn-group">
        <div>
            <button class="btn btn-outline-info">{{ button_label|default('Enregister') }}</button>

        </div>

        <div>
            {{ include('manager/suites/_delete_form.html.twig') }}
        </div>
    </div>

    <div>
        <a href="{{ path('manager_establishments_list') }}" class="btn btn-outline-info">Retour à la liste</a>
    </div>

{% endblock %}
