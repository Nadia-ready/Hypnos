<form method="post" action="{{ path('suites_list_delete', {'id': suite.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cette suite?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suite.id) }}">
    <button class="btn btn-outline-info">Supprimer</button>
</form>
