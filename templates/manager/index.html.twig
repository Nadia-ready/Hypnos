{% extends 'admin/base.html.twig' %}

{% block title %}Manager index{% endblock %}

{% block main %}
    <h1>Manager index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Lastname</th>
                <th>Email</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for manager in managers %}
            <tr>
                <td>{{ manager.id }}</td>
                <td>{{ manager.name }}</td>
                <td>{{ manager.lastname }}</td>
                <td>{{ manager.email }}</td>
                <td>{{ manager.password }}</td>
                <td>
                    <a href="{{ path('manager_list_show', {'id': manager.id}) }}">show</a>
                    <a href="{{ path('manager_list_edit', {'id': manager.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('manager_list_new') }}">Create new</a>
{% endblock %}
